/**
 * 共通線画プロンプト（全カテゴリ共通）
 */
export function generateLineArtPrompt(): string {
  return `**【重要】この画像を厳密に白(#FFFFFF)と黒(#000000)の2色のみで構成されたモノクロ画像に変換してください。青色やグレーは絶対に使用禁止です。**

🎯 **この作業の目的**: アップロード画像を白い背景に黒い線だけの線画に変換

**【線画変換の厳格なルール】**
1. **使用可能な色は2色のみ**：
   - 背景：純白（#FFFFFF）
   - 線：純黒（#000000）

2. **絶対に使用禁止の色**：
   - グレー（#808080, #C0C0C0など）
   - 青色（#0000FF, #4169E1など）
   - その他の中間色

3. **線画の品質要件**：
   - 輪郭線を明確に描く
   - 細部の線も保持する
   - 線の太さは均一にする
   - 元画像の構図・比率を正確に保持

4. **背景処理**：
   - 背景は完全に白（#FFFFFF）にする
   - 背景の色や模様は全て除去

**【最終確認】**
- 画像が白と黒の2色のみで構成されているか？
- グレーや青色が使用されていないか？
- 元画像の形状が正確に保持されているか？
- 背景が純白になっているか？

**重要：この線画は後続のステップで色塗りの基礎となるため、正確な輪郭線と純白の背景が必須です。**`;
}
